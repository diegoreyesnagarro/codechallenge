Instructions: 
this project is configured to connect to a SQL server instance and Vehicles Database
it contains a SQL script to create the table schema to operate.

React app needs to run in 3000 port in order to avoid having CORS responses.
Service needs to run in port 44338


http://localhost:3000/ file Upload
http://localhost:3000/dealView deal view
